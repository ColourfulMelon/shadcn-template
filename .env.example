# Copy this file to .env and fill in the values.

# Canonical site URL, including protocol, without a trailing slash.
# Optional on Vercel: falls back to https://$VERCEL_PROJECT_PRODUCTION_URL,
# then to http://localhost:3000.
SITE_URL=http://localhost:3000
